# Root dependency manifest for the Vercel Python build.
# Flattened from requirements/base.txt because Vercel's builder does not
# resolve `-r` include directives. Keep versions in sync with that file.
Django>=5.0,<5.1
djangorestframework>=3.15,<3.16
djangorestframework-simplejwt>=5.3,<5.4
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.0,<2.0
gunicorn>=23.0,<24.0
google-auth>=2.35,<3.0
requests>=2.32,<3.0
razorpay>=2.0,<3.0
celery>=5.4,<6.0
redis>=5.0,<6.0
